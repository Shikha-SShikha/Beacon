# Next Steps

> **Archived** — this doc described the staged enrichment POC, which is now complete.
> Current open items live in `Beacon-docs/wiki/open-items.md`.

---

## What was planned here (done)

The staged enrichment POC: `stage_copyedit.py` → human review (`review_app.py`) → `stage_mastercopy.py`.

Both XML files (`Copyedit.xml`, `Proof .xml`) were processed. Manifests are at `.tmp/enrichment_copyedit.json` and `.tmp/enrichment_mastercopy.json`.

See `Beacon-docs/wiki/staged-enrichment.md` for the current reference.
